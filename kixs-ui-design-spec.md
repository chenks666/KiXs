# KiXs · 界面设计规格说明书
**版本**：v2.0（无 AI 味重构）
**设计师**：UI Designer（20 年从业）
**日期**：2026-08-09

---

## 1. 设计哲学：什么才是「无 AI 味」

AI 生成的界面有一个通病——**用「装饰密度」冒充「设计质量」**。凡是看起来"很用力"却经不起细看的界面，几乎都是同一套模板产物。KiXs 新设计遵循五条铁律：

| # | 铁律 | 反例（现状） | 正解 |
|---|------|------------|------|
| 1 | **克制优先** | 每屏都在发光、悬浮、旋转 | 一个页面只允许 1 个视觉焦点 |
| 2 | **留白是材料** | 卡片贴卡片，间距 20px 均匀到底 | 按 8pt 节奏分级留白，大留白制造呼吸感 |
| 3 | **材质真实感** | 统一毛玻璃 + 彩色光晕 | 每板块一种「器物质感」：纸墨 / 青铜 / 星图 / 鎏金 |
| 4 | **中文有根** | 大字距（3-4px）+ 无衬线堆叠 | 中文方块字不放大字距；标题用衬线（宋/楷），UI 用无衬线 |
| 5 | **动效是注解** | 无限旋转、逐个上浮 | 动效只发生在「状态变化」，150-250ms，一次一个 |

**一句话**：让用户感觉「这个产品是有人认真做出来的」，而不是「AI 三分钟拼出来的」。

---

## 2. AI 味诊断（现状问题清单）

基于对现有界面的逐屏审查：

1. **英文 kicker + 居中大字 hero**——AI 落地页模板特征，首页整页都是"标语+按钮"结构
2. **能量分数卡（78/100 + 进度条）**——把玄学硬套 KPI 仪表盘，最假的部分
3. **旋转魔法阵 + 浮动星点 + 常亮动画**——装饰性动画堆砌
4. **中文 3-4px 字距**——西文习惯强加中文，字间距散架
5. **渐变文字（background-clip:text）**——廉价渐变装饰
6. **统一 20px 大圆角 + 胶囊按钮 + 毛玻璃**——泛化"现代感"，无文化根基
7. **hover translateY 上浮 + 光晕阴影**——机械微交互
8. **emoji 图标**——不专业，破坏精致感
9. **每板块都是同一套深色+主色**——氛围趋同

---

## 3. 设计系统 v2.0

### 3.1 色彩系统（语义化）

全局中性色（所有板块共用基底）：

```
--ink-900  #1a1613   近墨黑（最深文字/背景）
--ink-700  #4a443c   次级文字
--ink-500  #8a8278   弱文字/占位
--ink-300  #b8b2a8   分割线
--paper    #faf6ee   纸色（浅色板块基底）
```

每板块一组「主色 + 材料色」双轴：

| 板块 | 氛围 | 主色 | 材料色（质感） | 视觉语言 |
|------|------|------|--------------|---------|
| 首页 | 月蚀 | 薰衣草紫 `#9d8ec9` | 深空靛 `#14101f` | 收敛后的星空，减光晕 |
| 命术 | 青铜罗盘 | 古铜金 `#c8a24a` | 墨绿铜 `#16201c` | 罗盘刻度、细线 |
| 占术 | 深紫鎏金 | 鎏金 `#cfa954` | 暗紫 `#191130` | 牌面为主、大留白 |
| 八字 | 纸墨朱印 | 朱砂 `#b03a2e` | 宣纸米 `#efe6d4`（浅色） | 四柱表格、楷体标题 |
| 紫微 | 星图青金 | 星青 `#7fb8a6` | 玄青 `#0d1620` | 九宫格、天文网格 |
| 星盘 | 深空星图 | 天文蓝 `#7fa8d8` | 墨蓝 `#0b1020` | 圆形刻度、细线条 |
| 塔罗 | 深紫鎏金 | 鎏金 `#cfa954` | 深紫 `#1a1133` | 牌面大图、留白 |
| 梅花 | 素纸水墨 | 黛青 `#5a7a8a` | 素纸 `#f3eee2`（浅色） | 卦爻线、印章 |
| 六爻 | 墨朱竹简 | 朱砂 `#b03a2e` | 墨黑 `#171310` | 爻线粗细、竹简纹理 |
| 六壬 | 藏蓝古铜 | 古铜 `#c8a24a` | 藏蓝 `#0e151c` | 罗盘、天地盘线 |

### 3.2 字体系统

```
UI 字体：      PingFang SC / HarmonyOS Sans SC / system-ui（无衬线，字号 12-16 常规）
标题字体：    STSong / Songti SC / Noto Serif SC（宋体，页面大标题用）
内容气质字体：STKaiti / KaiTi（楷体，仅用于干支/卦名/诗句等「内容本体」）
英文辅助：    Avenir Next / DIN（仅标签角标，字距 ≤1px）
```

**中文字距规则**：正文与标题 `letter-spacing: 0`（中文天然方块，无需字距）；仅英文角标允许 ≤1px。

**字号层级**：
```
--text-hero   40px / 48px（仅首页主标）
--text-h1     28px（板块页标题，宋体）
--text-h2     20px（卡片标题）
--text-h3     16px（小节）
--text-body   14px（正文）
--text-caption 12px（辅助/标签）
```

### 3.3 间距系统（8pt 网格）

```
--space-1  4px    --space-2  8px
--space-3  12px   --space-4  16px
--space-6  24px   --space-8  32px
--space-10 40px   --space-16 64px
```

**节奏原则**：区块间用 space-16（64px）制造段落感；卡片内用 space-6（24px）；相邻元素 space-3/4。

### 3.4 圆角 / 阴影（器物感）

```
--radius-sm  6px   （按钮、输入框——器物棱角，非胶囊）
--radius-md  10px  （小卡、标签）
--radius-lg  14px  （卡片、面板）
--radius-xl  18px  （仅弹层/大面板）
```

**阴影改为「单一低透明度深色」**，取消彩色光晕：
```
--shadow-sm: 0 1px 3px rgba(26,22,19,.12)
--shadow-md: 0 4px 12px rgba(26,22,19,.16)
--shadow-lg: 0 12px 32px rgba(26,22,19,.22)
```

### 3.5 组件规范

#### 按钮（器物感）
```
.btn {
  高度 40px；圆角 6px（非胶囊）；边框 1px 主色 40%；
  常态：透明底 + 主色文字
  强调：主色实底 + 深色文字
  禁用：40% 透明度
  hover：主色 10% 底（不位移、不发光）
}
```

#### 卡片（纸张/器物感）
```
.card {
  圆角 14px；边框 1px line；背景 panel（或纸色）；
  无光晕阴影；hover 仅边框提亮（不位移）
}
```

#### 标签/徽章（印章感）
```
.tag {
  圆角 2px（近方形，如印章）；1px 边框；字距 1px；
  深色板块用主色字，浅色板块用墨色字
}
```

#### 表单输入
```
.input {
  圆角 6px；边框 1px line；聚焦：边框主色 + 无外发光
}
```

#### 底部 Tab
```
固定底栏；纯色底 92% + 顶部 1px 分割线（去毛玻璃）；
选中态：主色小圆点（8px）替代全宽高亮条
```

### 3.6 动效规范（克制）

```
时长：150-250ms，ease-out
允许：状态切换的淡入淡出、位移 ≤8px
禁止：常亮旋转、无限循环、弹性回弹
prefers-reduced-motion: 全部降为 0 过渡
```

---

## 4. 各板块界面设计要点

### 4.1 首页（月蚀·收敛版）
- 去旋转魔法阵 → 静态「月蚀圆环」器物纹（一圈细环 + 一枚徽印）
- 去能量分数卡 → 改为「今日一签」诗句卡（一句话 + 一枚小印），不伪造数据
- 去英文 kicker 堆砌 → 只保留 1 个品牌角标
- 主标题宋体，两行，无渐变
- 师傅卡：器物式小像 + 名号，不悬浮

### 4.2 八字（纸墨朱印）
- 浅色宣纸底（#efe6d4），墨字，朱砂印章点缀
- 四柱以「竖排卷轴」呈现（传统命书样式）
- 大运/流年用细线时间轴，不用彩色柱状图

### 4.3 紫微（星图青金）
- 玄青底 + 青金细线九宫格
- 十二宫为「宫格卡片」，宫名用宋体小标题
- 四化徽章改为「禄/权/科/忌」朱文/白文小印

### 4.4 星盘（深空星图）
- 墨蓝底 + 细白刻度圆环（天文仪器感）
- 行星用「符号 + 数字」标注，不用大色块
- 相位线：实线吉、虚线张力

### 4.5 塔罗（深紫鎏金）
- 牌面为绝对主角：大图 + 充足留白
- 牌位用细鎏金线框，正逆位小印标注
- 牌背统一纹样（不随机花哨）

### 4.6 梅花 / 六爻（水墨 / 墨朱）
- 素纸底或墨黑底，卦爻线用「笔画」渲染（阳爻实线、阴爻断开）
- 动爻用朱砂小圆点标注
- 体用、吉凶结论用「印章」式徽标（红/黑小方印）

### 4.7 六壬（藏蓝古铜）
- 藏蓝底 + 古铜罗盘线
- 天地盘以同心圆呈现，四课三传用竖排文字

### 4.8 个人中心（中性·器物）
- 不再用渐变头像，改「徽印 + 名号」
- 列表：细分割线 + 无圆角卡片（iOS 原生感）
- 统计数字用 tabular-nums 等宽数字

---

## 5. 无障碍（WCAG AA）

- 正文文字对比度 ≥ 4.5:1（深色板验证通过）
- 交互目标 ≥ 44×44px
- 支持 `prefers-reduced-motion`
- 焦点可见（focus-visible 主色 2px）

---

## 6. 落地清单

| 优先级 | 改动 | 文件 |
|--------|------|------|
| P0 | 设计 token 重构（色彩/间距/圆角/阴影） | css/base.css |
| P0 | 首页去 AI 味重设计 | kixs/index.html |
| P1 | 组件样式重构（btn/card/tag/input/tab） | css/base.css |
| P1 | 板块主题 token 氛围强化 | css/base.css |
| P2 | 各排盘页细节适配（四柱/宫格/牌阵） | pages/*.html |

---
*本文档为 KiXs v2.0 界面设计规格，供实现与评审使用。*
